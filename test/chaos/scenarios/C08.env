# C08 parameters.
DURATION=3m
PRE_FAULT_S=10
POST_FAULT_S=15

SIMUL_CYCLES=10
SIMUL_SETTLE_S=20
GATEWAY_NAME=load
GATEWAY_NAMESPACE=varnish-load
ROUTE_NAMESPACE=varnish-load

# Pass criteria.
# Data path unaffected; zero tolerance for drops or misroutes on
# fixture routes.
MAX_DROP_RATIO=0.0
MAX_CONVERGE_MS=5000
MAX_MISROUTES=0
