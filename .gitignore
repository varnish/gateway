# IDE
.idea

# Build artifacts
dist/
/operator
/chaperone

# Test artifacts
coverage.out
*.report.log

# EnvTest binaries (platform-specific, don't commit)
testbin/
bin/setup-envtest

# Static analysis tool binaries (installed on demand by `make lint`)
bin/staticcheck
bin/nilaway
bin/govulncheck

# Internal working notes (not published)
docs/internal/

# MkDocs build output + virtualenv
/_site/
/.venv-docs/
